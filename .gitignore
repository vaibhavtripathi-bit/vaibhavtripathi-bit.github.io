# Dependencies (recreate anytime with `npm install`) — never committed
node_modules/

# Build output (generated by `npm run build`) — never committed
dist/
.astro/

# Environment / secrets
.env
.env.production

# OS / editor cruft
.DS_Store
*.log
npm-debug.log*
